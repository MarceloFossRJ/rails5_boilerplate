<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
    <!-- sidebar toggle -->
    <button class="navbar-toggler collapsed" data-toggle="collapse" data-target="#n" type="button" id="sidebar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="icon-bar top-bar"></span>
      <span class="icon-bar middle-bar"></span>
      <span class="icon-bar bottom-bar"></span>
    </button>

    <!-- title / brand -->
    <a class="navbar-brand navbar-brand-width " href="<%= current_user ? dashboard_path : root_path %>">ENV["APPLICATION_NAME"]</a>
      <div class="collapse navbar-collapse" id="n">
      </div>
  </nav>
</header>